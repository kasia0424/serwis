{% extends 'base.twig' %}

{% block content %}
    <h1 class="intro-text text-center">
        {{ 'Delete category' |trans }}
    </h1>
    <div>
        <h3>{{ 'Are you sure, that you want to delete this category?' |trans }}</h3>
        <h4>{{ 'All ads from it will be moved to Pozostałe category.' |trans }}</h4>
        <p> {{ '(yes for delete,' |trans }}<br/>
        {{ 'no for go back to categories page)' |trans }}</p>

        <form action="#" method="post">
            {{ form_widget(form) }}
        </form>
    </div>

{% endblock %}