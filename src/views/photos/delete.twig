{% extends 'base.twig' %}

{% block content %}
    <h1 class="intro-text text-center">
        {{ 'Delete photo' |trans }}
    </h1>
    <div>
        <h3>{{ 'Are you sure, that you want to delete this photo?' |trans }}</h3>
        <p> {{ 'Click yes for delete' |trans }}<br/>
        {{ 'no for go back to homepage.' |trans }}</p>

        <form action="#" method="post">
            {{ form_widget(form) }}
        </form>
    </div>

{% endblock %}